---
name: chinese-formal-writing
description: "打磨中文学术/公文类文稿的通用工序：去AI味与口语味（英文缩略语堆砌、双引号破折号泛滥、口语衔接词、工作汇报腔、程度副词滥用、口号式三四字并列、管理黑话、动宾搭配不当）、去列表化（把『目标:/(1)(2)』条目、加粗编号小标题、表格改写为主题句领起的连续段落）、术语漂移与跨节复读的机械审计（audit.py 一条命令报出同一概念多种叫法、同一判断跨章节逐字复现、引文编号乱序、孤图）、可读性与逻辑核查（循环定义、指代悬空、主语丢失、自相矛盾）、学术化分遍过稿（先机械后判断，每遍只查一类 + 改完回归复查）、导出符合中文公文格式的 docx（pandoc + python-docx 套字体字号行距、修中文引号）、以及逐段/分批审阅 prompt。适用于申报书、研究报告、方案、论文稿等任何需要学术书面语的中文文稿。当用户说『去AI味』『太像列表/把列表改成段落』『这段口语味太重』『术语前后不一致』『这段读不懂/别扭』『导出中文公文 docx』『帮我审阅这份中文稿』『标题太口号/有黑话』时适用。本 skill 是通用写作打磨层，不含任何特定文体的结构/章节规范。"
---

# 中文学术/公文写作打磨

## 用途

把一份中文文稿从"有 AI 味、像列表、口语化、格式不规范"打磨到"学术书面、成段论述、术语统一、可直接出公文 docx"。这里只管**通用的写作工序与出稿工具**，不绑定任何具体文体或学科——申报书、研究报告、方案、论文稿都能用。

**特定文体的结构规范（章节怎么排、重心怎么放、篇幅怎么控、某类申请书的固定栏目与字数）不在本 skill**，由对应的文体 skill 负责：
- 一般中文课题申报书 → `grant-proposal-writing`（主线重心、章节骨架、篇幅分配、拟题）。
- 中国博士后科学基金面上资助申请书 → `postdoc-fund-proposal`（固定六栏、字数上限、匿名评审红线）。

这些文体 skill 复用本 skill 做去AI味、去列表化、过稿、出稿、审阅；本 skill 也可**独立使用**于任何中文文稿的润色与出稿。

## 动手前先确认一件事：结构能不能动

**审阅或改稿开始前，先向用户确认结构的可改范围**，三选一：

| 模式 | 含义 | 影响 |
|---|---|---|
| 结构可动 | 章节顺序、段落切分、列表与表格都可改写 | 通用清单全套适用 |
| 仅改内容 | 骨架、编号体例、加粗位置照旧，只改措辞 | **去列表化整条停用**；"(1)(2) 条目""前者/后者""各段同构"不算问题 |
| 完全冻结 | 逐句润色，不动句序 | 只留用词与搭配层面的检查 |

这一步不能省。本 skill 的清单默认"结构可动"，会把 `**（1）…（2）…**` 判为列表残留、把"前者/后者"判为口头禅、把各段同构判为模板感——**但在很多文体里这些恰恰是硬体例**（例如按某类基金模板写的申报书，评审就指望着逐条数点）。不问清楚就套清单，会系统性地误伤，用户得回过头来纠正。

## 五类要治的问题

改稿时按五个方向排查，详细清单见 references：

1. **AI味与口语味**——英文缩略语堆砌、括号补丁/免责反复、表格矩阵过密、口号式三字/四字并列、双引号和破折号泛滥；口语化衔接词（"需要说明的是""在此基础上"）、工作汇报腔、段落首句非主题句、程度副词滥用、管理黑话、动宾搭配不当；以及最难自查的两类——**英文直译词**（"增量信息""未见编解码器""生成主干"，全是汉字但读者复述不出意思）与**名词压缩过度**（"开放环境边界""真假监督"，该有的虚词全省了）。详见 `references/去AI味核对清单.md`（18 类）。
2. **读不懂与自相矛盾**——循环定义、属概念错位、指代悬空、主语中途丢失、句子残缺、跨章节内部矛盾、正文与图表公式不一致。这是与"像不像 AI"不同的一根轴，**而且更严重**：文风差评审觉得糙，读不懂评审直接判你没想清楚。详见 `references/可读性核对清单.md`。
3. **像列表不像论述**——"目标:/主要内容:(1)(2)"式条目、加粗编号小标题、表格，读起来机械。改写为主题句领起、过渡句衔接的连续段落。**仅在"结构可动"模式下适用。**详见 `references/去列表化改写.md`。
4. **术语漂移与跨节复读**——同一概念多处用不同词、同一判断在不同章节逐字复现。这两类**占实测改动量的一半以上，且全部可机械发现**，用 `scripts/audit.py` 一条命令报出，不要靠肉眼。详见 `references/学术化过稿清单.md` 第 0 遍。
5. **格式不合规**——字体字号行距对不上公文要求、中文引号被 pandoc 转坏、孤图、引文编号不按出现顺序。详见 `references/格式与docx.md`。

## 文风定位

理想定位是**学术书面语承载务实内容**：概念定义严谨、方法描述可操作，不因学术化而空洞、不因口语化而随意。它既不是论文腔的堆砌，也不是工作汇报的复制。段落**首句必须是概括本段的主题句**，这是全篇是否"成论述"的分水岭。

## 典型用法

1. **单点润色**：用户给一段/一节，要求"去AI味""把这段改成段落""口语味太重"→ 对照 `去AI味核对清单.md`、`去列表化改写.md` 就地改。
2. **终稿过稿**：整稿写完后，按 `学术化过稿清单.md` **分遍专项检查**——每遍只查一类（口语词/三字并列/形容词/长句/术语一致/主题句/去列表化），不要一次通读求全。前置一张术语表统一口径。
3. **出 docx**：顺序为**先修引号 → pandoc 转 docx → python-docx 套格式**。引号必须在 pandoc 前修，否则 smart 会把中文引号全变右引号。完整命令与格式硬指标见 `references/格式与docx.md`，脚本在 `scripts/`。
4. **审阅**：逐段精修或分批审阅，用 `references/审阅prompt.md` 的可复用 prompt；多进程/多会话做第二视角。改完再审。

## 关键提醒

- **先机械、后判断**。第 0 遍（`audit.py`：术语漂移、跨节复读、引文顺序、孤图、体例计数）必须跑在逐段精修之前。顺序颠倒会返工——先精修完前几章、再发现全书术语不统一，统一口径时会把已定稿的措辞连带推翻。
- **改稿必然制造新问题，收尾要有回归遍**。实测一轮修改引入过四类新错：全局替换撞出同词重复、合稿留下旧版残句、删句带走了先行词或引文、换词与前句已有的同形词撞车。硬规则：每次全局替换后复读所有替换点前后各一句；每次删句后检查被删句里有没有别处依赖的先行词、引文、术语定义；合稿后重跑一次 `audit.py`。

- **去AI味的重头是口语味**，不只是英文缩略语。口语衔接词、汇报腔、非主题句首句、程度副词，是最常被评审点名却最容易被 AI 润色反复带回的。
- **术语全文统一**作为硬约束。改稿前先把关键术语约定成固定提法（同一概念一个词、有全称不随意简写），审阅时盯住，防止 AI 各段用词漂移。模板见 `学术化过稿清单.md`。
- **有意保留的结构标记不要删**：关键问题/创新点里的"第一/第二…"序数、进度里的"第一阶段…"是给读者数点、看时间轴用的，去列表化时保留（但写成主题句领起的段落，不是裸条目）。
- **提防反向膨胀**：去列表化、学术化会自然加长文字；AI 润色还倾向于把删掉的列表/术语/表格加回、越改越长、擅自新增数字或结论。每轮改完对比字数，把"不得膨胀、不得加回已删内容、不得新增事实"写进审阅约束。
- **docx 格式以文稿所属通知/模板为准**；无明确要求时用常见默认（一级标题黑体三号加粗、二三级楷体_GB2312 三号、正文仿宋_GB2312 三号、行距 28 磅 EXACTLY、两端对齐、首行缩进 2 字、黑色）。脚本参数按实际要求改。

## 配套资源

- `references/去AI味核对清单.md` —— AI味与口语味的 18 类具体来源与逐条对治。
- `references/可读性核对清单.md` —— 读不懂与自相矛盾的 8 类，每条附可执行的判定动作。
- `references/学术化过稿清单.md` —— 终稿前分遍专项检查的可执行流程 + 术语表前置。
- `references/去列表化改写.md` —— 列表/表格 → 主题句段落的改写手法。
- `references/格式与docx.md` —— pandoc + python-docx 完整出稿流程与格式硬指标。
- `references/审阅prompt.md` —— 逐段审阅 prompt、多进程审阅组织方式（模板以申报书为例，可换文体）。
- `scripts/format_docx.py` —— 套用中文公文字体/字号/行距/对齐/缩进/颜色。
- `scripts/fix_quotes.py` —— 把 ASCII 直引号按状态机转为成对中文引号。
- `scripts/audit.py` —— 第 0 遍全局机械审计：术语漂移、跨节复读、引文编号顺序、孤图与重复图题、体例计数、口语词计数。仅用标准库。

**可选增强**：若装有 `pandoc-docx-template` skill，可复用其 pandoc 模板与 Lua 过滤器做更精细的 docx 转换；未装则本 skill 的 pandoc + `format_docx.py` 基础流程即可独立出稿，不构成必要依赖。
